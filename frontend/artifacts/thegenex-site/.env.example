# Google Apps Script Web App deployment URL (see ../../README.md for setup steps).
# Leave unset for local development — the site falls back to mock mode:
# the contact form, analytics, and admin dashboard all work with local mock data.
VITE_API_URL=
