**/node_modules
**/dist
**/.git
**/.env
**/.env.*
!**/.env.example
**/*.tsbuildinfo
backend/.clasp.json
frontend/.replit-artifact
frontend/artifacts/*/.replit-artifact
frontend/.conversation

# Note: frontend/lib/db, lib/api-spec, lib/api-client-react, and
# artifacts/api-server are unused scaffold (see CLAUDE.md) but are declared
# pnpm workspace members that thegenex-site's package.json still references
# (@workspace/api-client-react) — `pnpm install` needs their package.json
# present to resolve workspace:* deps, so they are NOT excluded here.
